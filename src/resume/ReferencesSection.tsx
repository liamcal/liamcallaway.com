import { Text } from "@react-pdf/renderer";
import { Section } from "./Section";

export const ReferencesSection = () => {
    return (
        <Section title="References">
            <Text style={{ fontSize: 10 }}>Available on request</Text>
        </Section>
    );
};
